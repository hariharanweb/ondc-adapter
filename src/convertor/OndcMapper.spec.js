import {
  it, expect, describe,
} from 'vitest';

describe('OndcMapper', () => {
  it('should test if ondc id is mapped with platform id ', async () => {
    // const ondcMapper = new OndcMapper('src/resource/test/sample.csv');
    // const csvData = await ondcMapper.parseCSV();
    expect('one').toBe('one');
  });
});
