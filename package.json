{
  "name": "ondc-adapter",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/hariharanweb/ondc-adapter.git",
  "license": "MIT",
  "scripts": {
    "start": "node --experimental-specifier-resolution=node src/app.js",
    "test": "vitest --reporter verbose --globals",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src --fix"
  },
  "dependencies": {
    "eslint": "^8.36.0",
    "node-modules": "^1.0.1",
    "pre-commit": "^1.2.2",
    "vitest": "^0.29.2"
  },
  "devDependencies": {
    "eslint-plugin-react": "^7.32.2"
  },
  "pre-commit": [
    "lint"
  ]
}
